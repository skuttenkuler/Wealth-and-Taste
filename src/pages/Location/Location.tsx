import React from "react"

export interface ILocationPageProps {};

const LocationPage: React.FunctionComponent<ILocationPageProps> = (props) => {
    return (
        <div>Locaiton</div>
    )
};

export default LocationPage;
